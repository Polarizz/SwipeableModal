import SwiftUI

struct Grabber: View {
    var body: some View {
        Capsule()
            .fill(Color(.quaternaryLabel))
            .frame(width: 35, height: 4)
            .padding(.top, 7)
    }
}
